<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <ImageView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:src="@drawable/banner0"
            android:scaleType="fitCenter"
            android:contentDescription="@null"/>

        <android.support.v4.view.ViewPager
            android:id="@+id/vp_main_products"
            android:layout_width="match_parent"
            android:layout_height="@dimen/widget_product_card_height"
            android:paddingTop="@dimen/standard_top_padding"
            android:paddingBottom="@dimen/standard_top_padding"
            android:paddingLeft="@dimen/standard_side_padding"
            android:paddingStart="@dimen/standard_side_padding"
            android:paddingRight="@dimen/standard_side_padding"
            android:paddingEnd="@dimen/standard_side_padding"
            android:clipToPadding="false"/>

        <android.support.v7.widget.RecyclerView
            android:id="@+id/rv_categories"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>
    </LinearLayout>
</ScrollView>
